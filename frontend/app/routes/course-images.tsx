import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/card';

export default function CourseImagesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>镜像管理</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-center py-4 text-gray-500">镜像管理页面开发中...</p>
      </CardContent>
    </Card>
  );
} 