import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/card';

export default function CourseResourcesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>课程资源</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-center py-4 text-gray-500">资源页面开发中...</p>
      </CardContent>
    </Card>
  );
} 